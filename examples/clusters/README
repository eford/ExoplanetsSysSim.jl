# This is still in development.
# Check with Matthias He

How to use:
- To run one simulated catalog, update params_common.jl and then run generate_systems.jl.
- To run an optimizer on a model, run optimize.jl (or optimize_parallel.jl if one has multiple processors).
