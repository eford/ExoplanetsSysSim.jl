# This is still in development.
# Check with Matthias He

How to use:
- To run one catalog, update params_common.jl and then run generate_systems.jl.
- To run the optimizer on a model, run optimize.jl.
